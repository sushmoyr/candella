<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-image-a">
<header class="main">
    <h1 class="jumbo">Login</h1>
</header>
<main>
    <div class="main blur">
        <div class="container">
            <form method="post" action="/login">
                <label for="email">
                    <input class="input" type="text" id="email" name="email" placeholder="Email">
                </label><br>
                <label for="password">
                    <input class="input" type="password" id="password" name="password" placeholder="Password">
                </label><br>
                <button class="custom-button" type="submit">Login</button>
            </form>
            <% if (data !=null && data.code === 404){%>
                <p class="text-danger">
                    Incorrect email or password
                </p>
            <%} else if(data !=null && (data.code ===200 || data.code===201)) {%>
                <p class="text-sm-center"> <%=data.message%> </p>
            <% } else { %>
            <% } %>
            <p>
                Don't have an account?
                <a class="link" href="/register">Create one</a>
            </p>
        </div>
    </div>
</main>
</body>
</html>