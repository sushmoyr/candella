//Helper Models
const Error = require('./Error');
const Success = require('./Success');
const Reason = require('./Reason');
//DB Models
const User = require('./User');

module.exports = {
    Error, Success, Reason, User
}